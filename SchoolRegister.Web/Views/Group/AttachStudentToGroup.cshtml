@{
    ViewData["Title"] = "Attach Student to Group";
}
<h2>Attach Student to Group</h2>
<div class="row">
    <div class="col-md-4">
        <form asp-action="AttachStudentToGroup">
            <div class="form-group">
                <label>Student</label>
                <select name="StudentId" class="form-control"
                    asp-items="@ViewBag.StudentsSelectList"></select>
            </div>
            <div class="form-group">
                <label>Group</label>
                <select name="GroupId" class="form-control"
                    asp-items="@ViewBag.GroupsSelectList"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Attach" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<a asp-action="Index">Back to List</a>
