@model SchoolRegister.ViewModels.VM.AttachDetachSubjectToGroupVm

@{
    ViewData["Title"] = "AttachSubjectToGroup";
}

<h1>AttachSubjectToGroup</h1>

<h4>AttachDetachSubjectToGroupVm</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="AttachSubjectToGroup">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <input type="hidden" asp-for="SubjectId" class="form-control" />
                <span asp-validation-for="SubjectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Group</label>
                <select asp-for="GroupId" class="form-control" asp-items="@ViewBag.GroupSelectList"></select>
                <span asp-validation-for="GroupId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Attach" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-controller="Subject">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
